mixin m-borderLeftTitle01(tg)
	-
		var op = {
			tag:       tg.tag ? tg.tag : "div",
			baseName:  tg.baseName ? tg.baseName : "m-borderLeftTitle01",
			className: tg.className ? " " + tg.className : "",
		}
	#{op.tag}( class = op.baseName + op.className )
		span.is-title!= tg.title